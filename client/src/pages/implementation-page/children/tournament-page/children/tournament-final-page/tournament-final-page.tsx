import React, { memo } from 'react';

export const TournamentFinalPageComponent = () => {
  return (
    <div>final</div>
  );
};

export const TournamentFinalPage = memo(TournamentFinalPageComponent);